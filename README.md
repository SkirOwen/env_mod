# env_mod

Make sure you have folder `dataset` for the data  
and a folder `plots` for the plots

# requirements

- `python >= 3.8`
- `numpy`
- `matplotlib`
- `pandas`
- `statsmodels`
- `sklearn`
- `seaborn`
